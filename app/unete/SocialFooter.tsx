/**
 * Se conserva el nombre para no romper el import de /unete, pero el footer
 * ahora vive en components/SiteFooter.tsx y se comparte con la landing.
 */
export { SiteFooter as SocialFooter } from "@/components/SiteFooter";
